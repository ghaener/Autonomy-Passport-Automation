Commande pour installer un package:
	python -m pip install python-pptx

Ou le nom du package ici est python-pptx
Pb de version de python installée, où le pip installe sur une version qui n'est pas celle utilisée pour le run